version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - mongo

  mongo:
    image: mongo:latest
    ports:
      - "50000:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
